{
  "name": "seotool",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf ./build && babel ./src/seotool.js --out-dir ./build --no-comments --out-file-extension .min.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.14",
    "@babel/core": "^7.13.14",
    "@babel/preset-env": "^7.13.12",
    "babel-preset-minify": "^0.5.1"
  }
}
